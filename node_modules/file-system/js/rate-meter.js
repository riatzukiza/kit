type(String);
describe(String, init(path()));
type(Timer);
describe(RateMeter, Date.now().slice(-1)[0], init(emitter(event, callback(this.update.bind(this)))), gmth(start, emitter(event, timer(this), callback(->(timer.update()))), timer._running = true;
timer.callback = callback;, emitter.on(event)), gmth(stop, emitter(event, callback), timer._running = false;, emitter.removeListeners("event", callback)), gmth(restart, null), gmth(update, null, timer.last = Date.now();));
var defer = (function defer$(f, args) {
  /* defer ../file-system/sib/rate-meter.sibilant:25:0 */

  return =>(args());
});