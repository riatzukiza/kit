(macro p-test (promise  message ...body)

       `(.then (then-do @promise
                        (var message @message)
                        (print "###" "does" message "?")
                        (pipe  ...@body
                               (then value
                                     (print "####" message  )
                                     (print "#### succeeded,  proceeding to next test")
                                     )))))
